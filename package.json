{
  "name": "deep-readdir",
  "version": "0.1.0",
  "description": "List files in directory recursively",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "http://github.com/fedemp/deep-readdir/"
  },
  "keywords": [
    "readdir",
    "recursive"
  ],
  "scripts": {
		"build": "tsc",
		"jshint": "jshint index.js test/index.js",
		"pretest": "npm run build && npm run jshint",
		"test": "mocha"
  },
  "author": "fedemp",
  "license": "MIT",
  "devDependencies": {
    "jshint": "^2.7.0",
    "mocha": "^2.2.4",
    "typescript": "^1.5.0-beta"
  }
}
